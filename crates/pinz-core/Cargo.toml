[package]
name = "pinz-core"
description = "UI-agnostic core for pinz: domain model, world/screen projection, and the storage seam."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]

[features]
# Demo boards for tests and screenshots. Off by default, so the fixture is not
# compiled into a release binary.
fixtures = []
